<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
        }
        .container {
            max-width: 800px;
            margin: auto;
        }
        h1 {
            color: #333;
        }
        .success-message {
            color: green;
            margin-bottom: 20px;
        }
        .article {
            margin-top: 20px;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (message) { %>
            <p class="success-message"><%= message %></p>
        <% } %>

        <article class="article">
            <h1><%= blog.title %></h1>
            <p><em>By: <%= blog.author.first_name %> <%= blog.author.last_name %></em></p>
            <p><strong>Description:</strong> <%= blog.description %></p>
            <p><strong>Body:</strong></p>
            <p><%= blog.body %></p>
            <% if (blog.tags && blog.tags.length > 0) { %>
                <p><strong>Tags:</strong> <%= blog.tags.join(', ') %></p>
            <% } %>
        </article>

        <a href="/blogs/<%= blog._id %>/updateArticle">Edit this article</a> |
        <a href="/blogs/published">Back to all blogs</a> |   
        <a href="/blogs/my-articles">My Articles</a>

    </div>
</body>
</html>
